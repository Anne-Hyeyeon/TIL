# 2024-02-21
## export default {}
- JavaScript 및 ES6 (ECMAScript 2015)에서 export default {} 구문은 해당 파일에서 **단 하나의 항목(객체, 함수, 클래스 등)을 기본으로 내보내기 위해** 사용된다.

### export default {}를 사용하면 좋은 점
1. 명확한 진입점 제공
모듈이나 라이브러리에서 주요 기능이나 객체를 제공할 때, export default를 사용하면 해당 모듈의 "메인" 또는 "진입점"이 무엇인지 명확하게 표시할 수 있다.
2. 간결한 가져오기 구문
export default로 내보낸 항목은 가져올 때 중괄호 {} 없이 직접 이름을 지정하여 가져올 수 있어, 구문이 간결해진다.
```js
import MyDefaultExport from './MyModule';
``
3. 모듈 당 하나의 기본 내보내기
**각 모듈은 단 하나의 export default를 가질 수 있다.** 이는 모듈 설계 시 주요 기능이나 객체를 중심으로 구성하도록 유도하며, 모듈의 사용 방법을 명확하게 한다.
4. 호환성과 생태계 통합
다양한 JavaScript 라이브러리와 프레임워크에서 모듈 시스템을 사용하고 있으며, export default는 ES6 모듈 사양의 일부로 널리 지원된다. 이로 인해 코드의 호환성과 재사용성이 향상된다.
